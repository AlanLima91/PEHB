version: '2'
services:
  db:
    image: mysql:5.6
    environment:
      - MYSQL_ROOT_PASSWORD=root
  web:
    image: php:7.0-apache
    volumes:
      - .:/var/www/html
    links:
      - db
    ports:
     - "1111:80"